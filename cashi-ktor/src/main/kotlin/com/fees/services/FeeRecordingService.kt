package com.fees.services

import java.math.BigDecimal

class FeeRecordingService {
    fun recordFee(transactionId: String, fee: BigDecimal) {
        // Fee recording logic
        // Persist fee information to database or storage
    }
}